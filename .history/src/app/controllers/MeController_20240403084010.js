const Course = require("../models/Courses");
const { mongooseToObject } = require("../../util/mongoose");

class MeController {
  //todo [GET] /src/app/controllers/CourseController.js
  storedCourses(req, res, next) {

  }
}

module.exports = new CourseController();

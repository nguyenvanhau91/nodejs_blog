const Course = re

class SiteController {
    index(req, res) {
        res.json({
            name: 
        });

        // res.render("home");
    }

    search(req, res) {
        res.render("searchs");
    }
}

module.exports = new SiteController();
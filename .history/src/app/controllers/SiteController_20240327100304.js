const Course = require("../models/Courses");

class SiteController {
  a index(req, res) {
    try {
      const courses = async;
    } catch (error) {}
    // res.render("home");
  }

  search(req, res) {
    res.render("searchs");
  }
}

module.exports = new SiteController();
